# Ultralytics overrides

These files are the minimal source changes required by SCA-YOLO.

Apply them to an installed `ultralytics==8.3.235` before running the scripts in this repository.

Use `tools/apply_ultralytics_patch.ps1` to copy the files into the installed package on Windows.
